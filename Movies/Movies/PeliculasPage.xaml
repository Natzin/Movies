<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Movies.PeliculasPage">
    <StackLayout
        Padding="20">
        <ListView
            x:Name="lvMovies"
            BackgroundColor="#2196f7">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout
                            Orientation="Vertical">
                            <Label
                                Text="{Binding title}"
                                BackgroundColor="#2196f7"
                                TextColor="White"
                                FontSize="Large"/>
                            <Label
                                Text="{Binding category}"
                                BackgroundColor="#2196f7"
                                TextColor="White"
                                FontSize="Medium"/>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>